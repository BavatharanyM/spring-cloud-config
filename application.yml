erver:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BavatharanyM/spring-clous-config
          search-paths:
           - '*service'
